import React, { ReactElement, Component, FC} from 'react';
import {
  Route,
  Switch
} from 'react-router-dom';

interface Props {}
const Home = ({}: Props): ReactElement => {
  return <>Home</>
}

export default Home;