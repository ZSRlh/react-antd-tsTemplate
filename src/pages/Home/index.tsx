import React, { ReactElement } from 'react';

interface Props {}
const Home = ({}: Props): ReactElement => {
  return <div
    style={{
      height: '100%'
    }}
  >
    Home
  </div>
}

export default Home;